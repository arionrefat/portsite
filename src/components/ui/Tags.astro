---
export interface Props {
	tags?: string[];
}

const { tags } = Astro.props;
---

{
	tags && (
		<div class="flex flex-row flex-wrap justify-between">
			{tags.map((tag) => (
				<span class="inline-block rounded py-2 text-xs text-black  lg:py-1">#{tag}</span>
			))}
		</div>
	)
}
